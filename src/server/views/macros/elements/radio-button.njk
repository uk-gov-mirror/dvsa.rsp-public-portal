{% macro radio(text='', value='', id='', name='', checked='') %}
  <div class="multiple-choice">
    <input {{ 'id="' + id + '"' if id }} type="radio" name="{{ name if name else id }}" {{ 'value="' + value + '"' if value }} {{ 'checked=' if checked }}>
    <label {{ 'for="' + id + '"' if id }}>{{ text }}</label>
  </div>
{% endmacro %}§